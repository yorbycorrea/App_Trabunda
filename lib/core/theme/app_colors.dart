import 'package:flutter/material.dart';

/// Colores compartidos dentro de la aplicación.
class AppColors {
  const AppColors._();

  /// Color de fondo utilizado en las pantallas del escáner.
  static const Color scannerBackground = Color(0xfffafafa);
}